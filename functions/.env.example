# ID da Google Sheet que recebe a agenda semanal (ver PROJECT_SPEC.md).
# Não é segredo — crie um arquivo `.env.<project-id>` (ex.: .env.agenda-chromebooks-cahf)
# com o valor real; o `firebase deploy` carrega esse arquivo automaticamente.
SHEET_ID=
